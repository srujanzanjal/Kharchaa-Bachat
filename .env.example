# Server-side PostgreSQL Connection URI (Supabase or standard PostgreSQL)
# Keep DATABASE_URL strictly server-side — NEVER prefix with NEXT_PUBLIC_
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres"
